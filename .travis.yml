sudo: required
language: php
php:
  - 7.1

services:
  - mysql

cache:
  directories:
    - $HOME/.composer/cache/files

matrix:
  include:
    php: 7.2
    env:
      - CODE_COVERAGE=1

before_install:
  - ./travis_before_install.sh

before_script:
  - ./travis_before_script.sh

script:
  - ./travis.sh